# 2026-08-12

## 1. 根 README 生成
- 根据 8 个子项目的自带说明，撰写了根目录 `README.md` 的项目总览（RAG / 自主修复 / 客服 / 深度研究 / 企业平台 / Hermes 自进化 / MCP 中台 / Text-to-SQL）。

## 2. 逐项目 .gitignore 配置 + 清理已提交敏感/产物文件
- 为以下 10 个目录创建/更新了 `.gitignore`（通用 Python 规则 + 各项目特例）：
  agentic_rag_knowledge_assistant_v3, autonomous_repair_loop_agent, autonomous_repair_loop_agent_v1,
  customer_service_agent, deep_research_agent_v3, enterprise-agent-platform, eval_exercise,
  hermes_agent_system_demo, personal_mcp_assistan, text_to_sql。
- 通用忽略：__pycache__、*.pyc、.venv/、.env（保留 .env.example）、*.log、logs/、*.db/*.sqlite、.pytest_cache、.idea/.vscode、.DS_Store。
- 各项目特例：
  - agentic_rag: 忽略 `tmp_data/` 与自动生成的评测报告 json（保留 data/ 知识库文档与 eval_dataset.json）。
  - autonomous_repair(_v1): 忽略 `tmp_test.py` / `tmp_*`。
  - customer_service: 忽略 `src/nodes/tmp_intent_node.py`（保留 data/generate_mock_data.py 源码）。
  - deep_research: 忽略 `output/`（生成报告）。
  - hermes: 忽略 `skills/custom_skills/generated_learned_skill.py`（运行时生成）。
  - personal_mcp: 重写既有 .gitignore，保留 image.png（README 引用）。
  - eval_exercise: 额外忽略 `.deepeval/`、`eval_outputs/`。
- 关键操作：仓库原本已把 6 个 `.env`（含密钥）、多个 `*.db`/`*.log`/`__pycache__` 提交进了 git。用
  `git ls-files --cached --ignored --exclude-standard -z | xargs -0 git rm --cached`
  取消了这些文件的跟踪（文件保留在磁盘，仅移出索引），最终 `git ls-files --cached --ignored` = 0。
- 注意：示例数据库 chinook.db / northwind.db / customer_store.db 等按 `*.db` 规则忽略，克隆后需通过各自脚本/下载重新获取。
- 当前 git 状态：9 个 `.gitignore` 为未跟踪（??），源码已暂存（A），需在提交前 `git add` 这些 .gitignore。
